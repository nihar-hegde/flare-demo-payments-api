export async function processRefundTransaction(transactionId: string): Promise<void> {
  // Simulate an async operation before completing
  await new Promise((resolve) => setTimeout(resolve, 50));

  void transactionId;
}
